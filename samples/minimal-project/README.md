# OWS Minimal Sample

This tiny text-first project is used for onboarding and package smoke checks.

From this directory:

~~~text
ows init
ows package
ows verify
ows inspect
~~~

The package root is the reproducible logical identity. ZIP container timestamps
and the generated package identifier are metadata and are not part of that
identity.
